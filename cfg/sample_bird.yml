CONFIG_NAME: 'bird_basic'
DATASET_NAME: 'bird'
DATA_DIR: '../data/birds/'
SAVE_DIR: '../data/EE_GAN_OUT/visualization'
WORKERS: 4

TREE:
    BRANCH_NUM: 3
    BASE_SIZE: 64

TRAIN:
    BATCH_SIZE: 32  # 24
    CLASS_NUM: 200 # bird
    USE_CLASS: True
    MAX_EPOCH: 700
    NET_G: ''
    WARMUP_EPOCHS: 400
    GSAVE_INTERVAL: 20
    DSAVE_INTERVAL: 50

TEXT:
    EMBEDDING_DIM: 256
    CAPTIONS_PER_IMAGE: 10
    DAMSM_NAME: ''

GAN:
    GF_DIM: 48
    DF_DIM: 48